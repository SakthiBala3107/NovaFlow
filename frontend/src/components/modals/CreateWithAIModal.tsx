import type { AIModalProps } from "../../types/date.types"

const CreateWithAIModal = ({ isOpen, onclose, invoiceId }: AIModalProps) => {
    return (
        <div>CreateWithAIModal</div>
    )
}

export default CreateWithAIModal